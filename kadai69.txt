まず最初の行で「a=100」と定義される。
次の行では、まずhがラムダ閉包として定義される。さらにその中でフレームが増え、入れ子となるラムダ閉包が定義される。この時点での環境をラムダ閉包は含むので「a=100」という環境もこの時点でラムダ閉包の中に定義されている。
次の行では、let文によって新しくフレームが増え、「a=0」という定義がされる。次に、((h 1) (+ a 5))の式が実行される。(h 1)では(lambda (x) (lambda (y) (+ x (* 100 y))))にx=1という定義がなされ実行される。その結果、(lambda (y) (+ 1 (* 100 y))) という評価結果になる。さらに((h 1) (+ a 5))ではまず、(+ a 5)が実行され、a+5=0+5=5という結果になる。その結果(lambda (y) (+ 1 (*100 y)))にy=5という定義がなされ実行される。その結果(+ 1 (*100 5)) = 501という結果になる。
