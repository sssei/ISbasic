関数if-funでは引数を三つ取っている。しかし、Schemeでは評価戦略として「値呼び」をしており、引数を関数の中に入れるまえにその引数を評価する必要がある。したがって、関数「fact3」内でif-funを使用する時、引数である(< n 1)と１と(* n (fact3 (- n 1)))を関数に入れる前に先に評価する必要がある。ここで(* n (fact3 (- n 1)))を評価する際、再び関数内でif-funを使用する必要があり、これは再帰的に無限に続く。したがってこれを評価しても答えが返ってくることはない。
